# Map Upgrade header to connection upgrade value (required for WebSocket proxying)
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# Redirect HTTP -> HTTPS
server {
    listen 80;
    server_name _;
    return 301 https://$host$request_uri;
}

# Nginx metrics endpoint for exporter (internal use)
server {
    listen 8080;
    server_name _;

    location /stub_status {
        stub_status on;
        allow 127.0.0.1;
        allow 172.0.0.0/8;
        deny all;
    }
}

# HTTPS — main server block
server {
    listen 443 ssl;
    server_name _;

    ssl_certificate     /etc/ssl/certs/proctolearn.crt;
    ssl_certificate_key /etc/ssl/private/proctolearn.key;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Frontend (Next.js)
    location / {
        proxy_pass         http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Backend API
    location /api {
        proxy_pass         http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   Host $host;
    }

    # WebSocket (proctor)
    location /socket.io {
        proxy_pass             http://localhost:4000;
        proxy_http_version     1.1;
        proxy_set_header       Upgrade $http_upgrade;
        proxy_set_header       Connection $connection_upgrade;
        proxy_set_header       Host $host;
        proxy_read_timeout     3600s;
        proxy_send_timeout     3600s;
    }
}
